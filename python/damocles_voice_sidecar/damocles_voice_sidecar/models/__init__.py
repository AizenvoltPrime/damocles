"""Model assets bundled with the sidecar + manifest helpers.

Contains:
- ``MODEL_MANIFEST.json`` — the canonical list of downloadable models
- ``wake/*.onnx`` — bundled wake-word + preprocessor models
- ``manifest`` module — Python-side ``model_version_dir`` matching the
  TypeScript downloader's path convention
"""
